<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>NSAppTransportSecurity</key>
	<dict>
		<key>NSAllowsArbitraryLoads</key>
		<true/>
		<key>NSAllowsLocalNetworking</key>
		<true/>
		<key>NSExceptionDomains</key>
		<dict>
			<key>googlevideo.com</key>
			<dict>
				<key>NSExceptionAllowsInsecureHTTPLoads</key>
				<true/>
				<key>NSIncludesSubdomains</key>
				<true/>
			</dict>
			<key>192.0.2.1</key>
			<dict>
				<key>NSExceptionAllowsInsecureHTTPLoads</key>
				<true/>
				<key>NSIncludesSubdomains</key>
				<false/>
				<key>NSExceptionMinimumTLSVersion</key>
				<string>TLSv1.0</string>
			</dict>
		</dict>
	</dict>
	<key>ARIA_HOMELAB_HOST</key>
	<string>192.0.2.1</string>
	<!-- Backend API key sent as the X-API-Key header. Empty = no auth (opt-in,
	     off by default). Set your real key here locally and `git update-index
	     --assume-unchanged` this file so it isn't committed; also set
	     ARIA_API_KEY in the backend env to actually enforce auth. -->
	<key>ARIA_API_KEY</key>
	<string></string>
	<!-- Full backend base URL override (e.g. https://aria.example.com). Empty =
	     ignored; the app then falls back to http://ARIA_HOMELAB_HOST:8000. Set
	     this for TestFlight/App Store builds where Tailscale isn't available.
	     Release builds use Aria---Music-Browser-Info-Release.plist instead. -->
	<key>ARIA_BACKEND_URL</key>
	<string></string>
	<!-- App uses only exempt encryption (HTTPS/ATS); pre-answers the App Store
	     Connect export-compliance question. -->
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>
	<key>UIBackgroundModes</key>
	<array>
		<string>audio</string>
	</array>
</dict>
</plist>
